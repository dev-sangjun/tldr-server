const router = require("express").Router();

router.get("/", (req, res) => {
  //display all posts
});

router.get("/:id", (req, res) => {
  //display a post
});

router.post("/", (req, res) => {
  //display all posts
});

module.exports = router;
